# Backend Mediator Endpoints

## Objective

Migrate the existing Campaigns HTTP-to-application dispatch boundary to MediatR and correct its HTTP adapter organization to use-case-oriented presentation files, while preserving Campaign API behavior.

## Problem and rationale

Campaign endpoint delegates inject concrete application handlers directly. As the module grows, this couples HTTP adapters to use-case implementation types and leaves one endpoint mapper responsible for unrelated route logic. Issue #13 requests an explicit command/query dispatch boundary and a documented decision.

## Authorized scope

- Issue #13 only: Campaigns endpoint architecture, MediatR command/query dispatch, use-case-oriented presentation adapters, tests, and supporting architecture documentation.
- Preserve existing Campaign HTTP routes, response bodies, OpenAPI contracts, and Problem Details codes.
- Do not add repositories, generic base handlers, MVC controllers, unrelated frontend work, or future capability scaffolding.

## Constraints

- Strict TDD mode: `dotnet test services/api/Lorekeeper.slnx --no-build --configuration Release`.
- Route: delegated direct. Trigger: implementation spans multiple non-trivial backend, test, and documentation files.
- Delivery strategy: ask-on-risk; forecast: one correction work unit, likely over the advisory 400 authored-line heuristic because it moves existing adapters and aligns their focused tests and documentation without changing behavior.

## Tasks

- [x] BE-01 — Correct ADR-003 and aligned architecture guidance to accept use-case-oriented Campaign presentation adapters. Reopened reason: the earlier single `CampaignEndpoints.cs` rationale contradicted Issue #13's preferred HTTP adapter structure and is invalid. Route: delegated. Acceptance: documented physical presentation split, bounded composition responsibility, and rejected generic framework. Checks: documentation readback passed. Evidence: ADR-003, `AGENTS.md`, `docs/04-architecture.md`, and `docs/10-roadmap.md` now name the accepted `Presentation/` adapters and small composition mapper. Commit: `9fc3388` (`fix(api): split campaign presentation adapters`).
- [x] BE-02 — Correct Campaign create/list/get HTTP adapter organization without changing MediatR dispatch or public behavior. Reopened reason: the completed implementation retained the large single `CampaignEndpoints.cs`, so it did not meet Issue #13's preferred use-case-oriented HTTP adapter structure. Route: delegated. Acceptance: endpoints use `ISender` from physical presentation adapters; composition owns only `/api/campaigns` group setup; no generic abstractions are introduced. Checks: strict TDD RED/GREEN evidence; backend build and focused tests. Evidence: `CampaignEndpoints.cs` was replaced with `Presentation/CreateCampaignEndpoint.cs`, `ListCampaignsEndpoint.cs`, `GetCampaignEndpoint.cs`, and `CampaignEndpointComposition.cs`; the existing route metadata, transport DTO, `ISender` messages, result mapping, and Problem Details values were moved unchanged. Commit: `9fc3388` (`fix(api): split campaign presentation adapters`).
- [x] BE-03 — Extend HTTP/application boundary coverage, run applicable backend checks, and reconcile task documentation. Route: delegated. Acceptance: Campaign behavior and Problem Details remain covered; all recorded checks have observed outcomes. Checks: formatter, Release build, focused Campaign tests, full backend suite. Evidence: `dotnet format services/api/Lorekeeper.slnx --verify-no-changes` passed; Campaign integration coverage is retained in `CampaignEndpointsTests`, but `dotnet test services/api/Lorekeeper.slnx --no-build --configuration Release --filter FullyQualifiedName~Campaign` was blocked because Docker was unavailable at `npipe://./pipe/docker_engine` (19 Unit tests passed; 27 Testcontainers integration tests could not start). `dotnet test services/api/Lorekeeper.slnx --no-build --configuration Release` was likewise blocked (20 Unit tests passed; 27 integration tests could not start). Commit: `455a30e` (`feat(api): dispatch campaign endpoints with MediatR`). Rollback: remove `CampaignRequestHandlerTests.cs`; no public contract test changes were made.
- [x] BE-04 — Split Campaign HTTP adapters into a physical `Presentation/` layer and prove composition maps them without changing HTTP behavior. Reason: corrective implementation for Issue #13 after ADR-003's invalid single-file rationale. Route: delegated. Acceptance: create/list/get endpoint definitions own metadata, binding, `ISender` dispatch, and result mapping; composition owns group setup only; focused tests preserve routes, names, OpenAPI metadata, DTOs, status/body, and Problem Details codes. Checks: RED: `dotnet test services/api/Lorekeeper.slnx --no-restore --configuration Release --filter FullyQualifiedName~CampaignEndpointCompositionTests` failed with CS0234 because `Api.Modules.Campaigns.Presentation` did not exist. GREEN: the same command passed 2 Unit tests after implementation. `dotnet format services/api/Lorekeeper.slnx --verify-no-changes` passed. `dotnet build services/api/Lorekeeper.slnx --no-restore --configuration Release` passed with 0 errors and 2 pre-existing SSH.NET NU1903 warnings. `dotnet test services/api/Lorekeeper.slnx --no-build --configuration Release --filter FullyQualifiedName~Campaign` passed 21 Unit tests but could not run 27 Campaign integration tests because Testcontainers could not reach Docker at `npipe://./pipe/docker_engine`. `dotnet test services/api/Lorekeeper.slnx --no-build --configuration Release` passed 22 Unit tests and 1 Integration test; the other 27 integration tests were blocked by the same unavailable Docker endpoint. Evidence: `CampaignEndpointCompositionTests` asserts the three established route names and patterns for both direct presentation composition and `CampaignsModule` mapping; existing `CampaignEndpointsTests` retains HTTP behavior coverage pending Docker. Commit: `9fc3388` (`fix(api): split campaign presentation adapters`). Rollback: revert only the presentation file moves, composition mapper, focused split tests, and architecture correction; keep the prior MediatR requests/handlers.
- [x] BE-05 — Reconcile the historical Block 01 OpenSpec architecture contradiction without rewriting its implementation record. Reason: `openspec/changes/block-01-campaign-workspace-slice/design.md` and `tasks.md` still prescribe no MediatR and a single `CampaignEndpoints.cs`, which ADR-003 and accepted Issue #13 supersede. Route: delegated. Acceptance: the historical guidance remains visible and is explicitly marked superseded; both documents link the supersession to ADR-003 and Issue #13; the reconciliation does not claim Block 01 originally used MediatR or the `Presentation/` structure; no product behavior or implementation scope changes. Checks: documentation readback passed; `git diff --check` passed; a local `Test-Path` check confirmed the ADR-003 target of both relative Markdown links exists. Evidence: both OpenSpec documents retain the original Block 01 instructions and introduce a post-Block 01 reconciliation notice naming the superseded architecture and the current ADR-003/Issue #13 direction. Commit: pending. Rollback: revert only this reconciliation entry and the two OpenSpec supersession notices.

## Progress and evidence

- Current branch: `feat/backend-mediator-endpoints`.
- Source baseline: `ef731f3`.
- Correction complete in `9fc3388` (`fix(api): split campaign presentation adapters`). Docker/Testcontainers integration verification remains blocked; rerun the focused Campaign and full backend test commands when Docker is available.
- BE-05 documentation reconciliation is verified locally and awaits its documentation work-unit commit.
