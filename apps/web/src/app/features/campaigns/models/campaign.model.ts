export interface Campaign {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
}

export interface CampaignDto {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
}
